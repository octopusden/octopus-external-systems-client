apiVersion: template.openshift.io/v1
kind: Template
metadata:
  name: teamcity-2022-template
objects:
  - apiVersion: v1
    kind: Pod
    metadata:
      name: ${DEPLOYMENT_PREFIX}-teamcity22
      labels:
        app.kubernetes.io/name: ${DEPLOYMENT_PREFIX}-teamcity22
    spec:
      restartPolicy: Never
      activeDeadlineSeconds: ${{ACTIVE_DEADLINE_SECONDS}}
      serviceAccountName: ${SERVICE_ACCOUNT_ANYUID}
      securityContext:
        runAsUser: 0
      volumes:
        - name: teamcity-2022-data
          persistentVolumeClaim:
            claimName: ${DEPLOYMENT_PREFIX}-teamcity-2022-datadir
        - name: teamcity-2022-logs
          persistentVolumeClaim:
            claimName: ${DEPLOYMENT_PREFIX}-teamcity-2022-logs
      containers:
        - name: teamcity22
          image: ${DOCKER_REGISTRY}/jetbrains/teamcity-server:${TEAMCITY_2022_IMAGE_TAG}
          ports:
            - containerPort: 8111
              protocol: TCP
          readinessProbe:
            httpGet:
              path: /login.html
              port: 8111
            initialDelaySeconds: 60
            periodSeconds: 10
          volumeMounts:
            - name: teamcity-2022-data
              mountPath: /data/teamcity_server/datadir
            - name: teamcity-2022-logs
              mountPath: /opt/teamcity/logs
  - apiVersion: v1
    kind: Service
    metadata:
      name: ${DEPLOYMENT_PREFIX}-teamcity22-service
    spec:
      ports:
        - port: 8111
          protocol: TCP
          targetPort: 8111
      selector:
        app.kubernetes.io/name: ${DEPLOYMENT_PREFIX}-teamcity22
  - apiVersion: route.openshift.io/v1
    kind: Route
    metadata:
      name: ${DEPLOYMENT_PREFIX}-teamcity22-route
    spec:
      port:
        targetPort: 8111
      to:
        kind: Service
        name: ${DEPLOYMENT_PREFIX}-teamcity22-service
parameters:
  - description: Unique deployment prefix
    name: DEPLOYMENT_PREFIX
    required: true
  - description: Active deadline seconds
    name: ACTIVE_DEADLINE_SECONDS
    required: true
  - description: Docker registry
    name: DOCKER_REGISTRY
    required: true
  - description: Tag of TeamCity image
    name: TEAMCITY_2022_IMAGE_TAG
    required: true
  - description: Service account name with SCC - anyuid
    name: SERVICE_ACCOUNT_ANYUID
    required: true