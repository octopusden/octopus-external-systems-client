name: Dependency Submission

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  dependencies:
    uses: octopusden/octopus-base/.github/workflows/common-gradle-dependency-submission.yml@2cb0a0c697e38ca6614fb93b3a03233b3413d475
    with:
      java-version: '21'
