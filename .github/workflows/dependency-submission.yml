name: Dependency Submission

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  dependencies:
    uses: octopusden/octopus-base/.github/workflows/common-gradle-dependency-submission.yml@v3.1.0
    with:
      java-version: '21'
