name: Gradle Release

on: workflow_dispatch

jobs:
  build:
    uses: octopusden/octopus-base/.github/workflows/common-java-gradle-release.yml@support-hybrid-flow
    with:
      flow-type: hybrid
      java-version: '11'
    secrets: inherit
